# Google Gemini API Key (Optional)
# If provided, this key can also be configured dynamically in the app's settings UI.
# If omitted, the application will use the local mock AI simulator engine.
VITE_GEMINI_API_KEY=
